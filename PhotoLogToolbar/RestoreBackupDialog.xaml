<UserControl x:Class="PhotoLogToolbar.RestoreBackupDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="400">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select a backup to restore:" FontWeight="Bold" Margin="0,0,0,10"/>

        <ComboBox Grid.Row="1" ItemsSource="{Binding AvailableBackupsDisplay}" 
                  SelectedItem="{Binding SelectedBackup}" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Click="OkButton_Click" MinWidth="75"/>
            <Button Content="Cancel" IsCancel="True" Margin="5,0,0,0" MinWidth="75"/>
        </StackPanel>
    </Grid>
</UserControl>
